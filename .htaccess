<IfModule mod_rewrite.c>
    RewriteEngine On

    # Storage files: route through public/index.php (no symlink on OVH)
    RewriteRule ^storage/(.*)$ public/index.php [L]

    # Everything else: forward to public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
